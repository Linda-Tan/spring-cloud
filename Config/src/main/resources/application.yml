server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Linda-Tan/spring-cloud
          username: linda-tan
          password: tyf926893
          searchPaths: config-repo
          